version: "3"

services:
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRE_USERNAME=docker
      - POSTGRE_PASSWORD=docker
      - POSTGRE_DATABASE=apisolid
